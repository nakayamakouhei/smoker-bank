<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= display_meta_tags(default_meta_tags) %>

    <%= yield :head %>

    <%# PWA用 manifest %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <!-- ファビコン設定 -->
    <link rel="icon" type="image/png" href="<%= asset_path('favicon.png') %>">
    <link rel="apple-touch-icon" href="<%= asset_path('favicon.png') %>">
    <link rel="shortcut icon" href="<%= asset_path('favicon.png') %>">

    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=M+PLUS+Rounded+1c:wght@400;700&display=swap" rel="stylesheet">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="min-h-screen flex flex-col bg-gradient-to-b from-gray-50 to-gray-200">
    <!-- 固定ヘッダー -->
    <div class="fixed top-0 left-0 w-full bg-white shadow z-50">
      <%= render "layouts/header" %>
    </div>

    <!-- ヘッダー分の余白を確保 -->
    <main class="flex-grow flex flex-col pt-20 px-4">
      <% flash.each do |type, message| %>
        <% next if message.blank? %>
        <% css_classes = case type.to_sym
                        when :notice
                          "bg-green-100 text-green-800 border-green-200"
                        when :alert, :error
                          "bg-red-100 text-red-800 border-red-200"
                        else
                          "bg-blue-100 text-blue-800 border-blue-200"
                        end %>
        <% next if controller_name == "profiles" %>
        <% auto_dismiss = (type.to_sym == :notice && message == "箱数をカウントしました") %>
        <div class="mx-auto mb-4 w-full max-w-xl rounded border px-4 py-2 text-sm shadow <%= css_classes %> <%= auto_dismiss ? 'flash-auto-dismiss opacity-100 transition-opacity duration-500' : '' %>"
             data-auto-dismiss="<%= auto_dismiss %>">
          <%= message %>
        </div>
      <% end %>

      <div class="flex-1 flex flex-col">
        <%= yield %>
      </div>
    </main>
    <%= render "layouts/footer" %>

    <!-- ローディングスピナー -->
    <div
      data-controller="loading"
      data-loading-target="overlay"
      class="hidden fixed inset-0 flex items-center justify-center bg-white/70 backdrop-blur-sm opacity-0 transition-opacity duration-300 z-[9999]">
      <span class="loading loading-spinner text-emerald-500 w-12 h-12"></span>
    </div>

    <script>
      document.addEventListener("turbo:load", function() {
        document.querySelectorAll('[data-auto-dismiss="true"]').forEach(function(el) {
          setTimeout(function() {
            el.classList.add("opacity-0");
            setTimeout(function() {
              if (el && el.parentNode) { el.parentNode.removeChild(el); }
            }, 500);
          }, 5000);
        });
      });
    </script>
  </body>
</html>
