<div class="flex flex-col items-center justify-center min-h-screen bg-gray-50 px-4">
  <div class="bg-white border border-gray-200 rounded-xl shadow p-6 w-full max-w-md text-center">
    <h1 class="text-2xl font-bold text-green-700 mb-4">ようこそ 👋</h1>
    <p class="text-gray-700 mb-6"><%= current_user.name.presence || current_user.email %> さん、ログイン中です。</p>
    <div class="space-y-2 text-sm text-gray-600">
      <p>これから機能を増やしていきます。</p>
      <p>まずは Items の seed を入れて一覧を作る予定。</p>
    </div>
    <div class="flex flex-col sm:flex-row gap-4 justify-center">
      <%= link_to "ログアウト", destroy_user_session_path, 
            data: { turbo_method: :delete }, 
            class: "px-6 py-3 bg-red-400 rounded text-white font-semibold" %>
    </div>
  </div>
</div>