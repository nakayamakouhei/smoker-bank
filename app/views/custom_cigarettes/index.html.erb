<div class="flex flex-col items-center flex-1 px-4 py-8">
  <div class="w-full max-w-md overflow-hidden rounded-2xl border border-gray-200 bg-white shadow-xl">
    <div class="bg-gradient-to-r from-emerald-500 to-emerald-600 px-6 py-6 text-left">
      <p class="text-xs font-semibold uppercase tracking-widest text-white/70">Custom Brand</p>
      <h1 class="mt-2 text-2xl font-bold text-white">オリジナル銘柄一覧</h1>
      <p class="mt-1 text-xs text-white/80">あなた専用の銘柄を作成・選択して記録できます。</p>
    </div>

    <div class="space-y-6 px-6 py-6">
      <div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
        <%= link_to "新しい銘柄を作成",
              new_custom_cigarette_path,
              class: "inline-flex items-center justify-center gap-2 rounded-lg bg-emerald-500 px-4 py-2 text-xs font-semibold text-white shadow transition hover:bg-emerald-400 cursor-pointer" %>
        <%= link_to "既存銘柄画面に戻る",
              select_cigarettes_path,
              class: "inline-flex items-center justify-center rounded-lg bg-white px-4 py-2 text-xs font-semibold text-emerald-600 shadow hover:bg-emerald-50" %>
      </div>

      <% if @custom_cigarettes.any? %>
        <ul class="divide-y divide-gray-200 rounded-xl border border-gray-100 bg-gray-50/70">
          <% @custom_cigarettes.each do |c| %>
            <li class="flex items-center justify-between gap-4 px-4 py-3">
              <div>
                <p class="text-sm font-semibold text-slate-900"><%= c.name %></p>
                <p class="text-xs text-slate-500"><%= number_to_currency(c.price, unit: '', precision: 0) %>円</p>
              </div>
              <%= button_to "この銘柄を選択",
                    select_custom_cigarette_path(c),
                    method: :patch,
                    class: "inline-flex items-center gap-2 rounded-lg bg-white px-3 py-1 text-xs font-semibold text-emerald-600 shadow hover:bg-emerald-50 cursor-pointer" %>
            </li>
          <% end %>
        </ul>
      <% else %>
        <p class="rounded-xl border border-dashed border-emerald-200 bg-emerald-50/80 px-4 py-6 text-center text-sm text-emerald-700">
          まだオリジナル銘柄は登録されていません。<br>まずは上のボタンから作成してみましょう。
        </p>
      <% end %>
    </div>
  </div>
</div>
