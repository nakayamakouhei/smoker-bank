<div class="flex flex-col items-center flex-1 px-4 py-8">
  <div class="w-full max-w-md overflow-hidden rounded-2xl border border-gray-200 bg-white shadow-xl">
    <div class="bg-gradient-to-r from-emerald-500 to-emerald-600 px-6 py-6 text-left">
      <p class="text-xs font-semibold uppercase tracking-widest text-white/70">Create Brand</p>
      <h1 class="mt-2 text-2xl font-bold text-white">オリジナル銘柄を作成</h1>
      <p class="mt-1 text-xs text-white/80">名前と価格を設定して、あなただけの銘柄を記録できます。</p>
    </div>

    <div class="space-y-6 px-6 py-6">
      <%= form_with model: @custom_cigarette, local: true, class: "space-y-4" do |f| %>
        <div class="space-y-2">
          <%= f.label :name, "銘柄名", class: "text-sm font-semibold text-slate-800" %>
          <%= f.text_field :name,
                class: "w-full rounded-lg border border-gray-300 px-3 py-2 text-sm shadow-sm focus:border-emerald-500 focus:outline-none focus:ring-1 focus:ring-emerald-500",
                placeholder: "例）マイオリジナル" %>
        </div>

        <div class="space-y-2">
          <%= f.label :price, "価格（円）", class: "text-sm font-semibold text-slate-800" %>
          <%= f.number_field :price, min: 0,
                class: "w-full rounded-lg border border-gray-300 px-3 py-2 text-sm shadow-sm focus:border-emerald-500 focus:outline-none focus:ring-1 focus:ring-emerald-500",
                placeholder: "例）580" %>
        </div>

        <div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
          <%= f.submit "登録する",
                class: "inline-flex w-full items-center justify-center gap-2 rounded-lg bg-emerald-500 px-4 py-2 text-sm font-semibold text-white shadow transition hover:bg-emerald-400 cursor-pointer sm:w-auto" %>
          <%= link_to "一覧に戻る",
                custom_cigarettes_path,
                class: "inline-flex w-full items-center justify-center rounded-lg bg-white px-4 py-2 text-sm font-semibold text-emerald-600 shadow hover:bg-emerald-50 sm:w-auto" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
