<!-- app/views/devise/registrations/new.html.erb -->

<div class="flex flex-col justify-center items-center flex-1 px-4">

  <!-- 枠 -->
  <div class="bg-white border border-gray-300 rounded-xl shadow-lg p-6 w-full max-w-md text-center">

    <!-- タイトル -->
    <h2 class="text-3xl sm:text-4xl font-bold text-green-700 mb-6">新規登録</h2>

    <!-- フォーム -->
    <%= form_with model: resource, url: registration_path(resource_name), class: "space-y-6" do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <!-- 名前 -->
      <div class="text-left">
        <%= f.label :name, "名前", class: "block text-sm font-medium text-gray-700" %>
        <%= f.text_field :name, autofocus: true, class: "mt-1 block w-full p-2 border rounded" %>
      </div>

      <!-- メールアドレス -->
      <div class="text-left">
        <%= f.label :email, "メールアドレス", class: "block text-sm font-medium text-gray-700" %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email",
              class: "mt-1 block w-full p-2 border rounded" %>
      </div>

      <!-- パスワード -->
      <div class="text-left">
        <%= f.label :password, "パスワード", class: "block text-sm font-medium text-gray-700" %>
        <% if @minimum_password_length %>
          <p class="text-xs text-gray-500">（<%= @minimum_password_length %>文字以上）</p>
        <% end %>

        <div class="relative">
          <%= f.password_field :password,
                autocomplete: "new-password",
                id: "password_field",
                class: "mt-1 block w-full p-2 border rounded pr-10" %>

          <!-- 👁 アイコン（helperから2種類まとめて出す） -->
          <button type="button"
                  class="absolute inset-y-0 right-0 flex items-center pr-3 text-gray-500 password-toggle-btn"
                  data-target="password_field">
            <%= eye_toggle_icons %>
          </button>
        </div>
      </div>

      <!-- パスワード（確認） -->
      <div class="text-left">
        <%= f.label :password_confirmation, "パスワード（確認）", class: "block text-sm font-medium text-gray-700" %>

        <div class="relative">
          <%= f.password_field :password_confirmation,
                autocomplete: "new-password",
                id: "password_confirmation_field",
                class: "mt-1 block w-full p-2 border rounded pr-10" %>

          <!-- 👁 アイコン（helperから2種類まとめて出す） -->
          <button type="button"
                  class="absolute inset-y-0 right-0 flex items-center pr-3 text-gray-500 password-toggle-btn"
                  data-target="password_confirmation_field">
            <%= eye_toggle_icons %>
          </button>
        </div>
      </div>

      <!-- 登録ボタン -->
      <div>
        <%= f.submit "登録する",
              class: "w-full bg-green-500 text-white py-2 rounded hover:bg-green-400 transition" %>
      </div>
    <% end %>

    <!-- リンク -->
    <div class="mt-4 text-sm">
      <%= link_to "すでにアカウントをお持ちの方はこちら", new_session_path(resource_name),
            class: "text-green-600 hover:underline" %>
    </div>
  </div>
</div>
